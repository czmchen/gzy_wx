<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yq.dao.UserMerchantsDao">

    <insert id="save" parameterType="com.yq.entity.UserMerchants">
    	<selectKey keyProperty="id" order="AFTER" resultType="java.lang.Long">
	      select LAST_INSERT_ID()
	    </selectKey>
		insert into tb_user_merchants(adcode,address,adname,citycode,cityname,discount,distance,email,entr_location,exit_location,groupbuy,mapId,indoor_map,lng,lat,name,pcode,photos,pname,postcode,rating,shopinfo,tel,type,website,openId,setType,markAdCode,custId) values(#{adcode},#{address},#{adname},#{citycode},#{cityname},#{discount},#{distance},#{email},#{entr_location},#{exit_location},#{groupbuy},#{mapId},#{indoor_map},#{lng},#{lat},#{name},#{pcode},#{photos},#{pname},#{postcode},#{rating},#{shopinfo},#{tel},#{type},#{website},#{openId},#{setType},#{markAdCode},#{custId})
    </insert>
    
</mapper>